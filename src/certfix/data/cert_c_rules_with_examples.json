{
  "version": "2016",
  "total_rules": 115,
  "categories": [
    {
      "id": "PRE",
      "name": "Preprocessor",
      "rules": [
        {"id": "PRE30-C", "title": "Do not create a universal character name through concatenation", "example": "##\\u concat"},
        {"id": "PRE31-C", "title": "Avoid side effects in arguments to unsafe macros", "example": "MACRO(i++)"},
        {"id": "PRE32-C", "title": "Do not use preprocessor directives in invocations of function-like macros", "example": "#ifdef inside MACRO()"}
      ]
    },
    {
      "id": "DCL",
      "name": "Declarations and Initialization",
      "rules": [
        {"id": "DCL30-C", "title": "Declare objects with appropriate storage durations", "example": "return &local_var"},
        {"id": "DCL31-C", "title": "Declare identifiers before using them", "example": "use before declaration"},
        {"id": "DCL36-C", "title": "Do not declare an identifier with conflicting linkage classifications", "example": "extern then static"},
        {"id": "DCL37-C", "title": "Do not declare or define a reserved identifier", "example": "_Reserved or __reserved"},
        {"id": "DCL38-C", "title": "Use the correct syntax when declaring a flexible array member", "example": "struct { int n; int arr[]; }"},
        {"id": "DCL39-C", "title": "Avoid information leakage in structure padding", "example": "memcpy struct with padding"},
        {"id": "DCL40-C", "title": "Do not create incompatible declarations of the same function or object", "example": "int f() vs void f()"},
        {"id": "DCL41-C", "title": "Do not declare variables inside a switch statement before the first case label", "example": "switch { int x; case: }"}
      ]
    },
    {
      "id": "EXP",
      "name": "Expressions",
      "rules": [
        {"id": "EXP30-C", "title": "Do not depend on the order of evaluation for side effects", "example": "a[i] = i++"},
        {"id": "EXP32-C", "title": "Do not access a volatile object through a nonvolatile reference", "example": "*(int*)&volatile_var"},
        {"id": "EXP33-C", "title": "Do not read uninitialized memory", "example": "int x; use(x)"},
        {"id": "EXP34-C", "title": "Do not dereference null pointers", "example": "*p without NULL check"},
        {"id": "EXP35-C", "title": "Do not modify objects with temporary lifetime", "example": "modify compound literal"},
        {"id": "EXP36-C", "title": "Do not cast pointers into more strictly aligned pointer types", "example": "(int*)char_ptr"},
        {"id": "EXP37-C", "title": "Call functions with the correct number and type of arguments", "example": "printf wrong args"},
        {"id": "EXP39-C", "title": "Do not access a variable through a pointer of an incompatible type", "example": "*(float*)&int_var"},
        {"id": "EXP40-C", "title": "Do not modify constant objects", "example": "*(int*)&const_var = 1"},
        {"id": "EXP42-C", "title": "Do not compare padding data", "example": "memcmp structs with padding"},
        {"id": "EXP43-C", "title": "Avoid undefined behavior when using restrict-qualified pointers", "example": "restrict ptr aliasing"},
        {"id": "EXP44-C", "title": "Do not rely on side effects in operands to sizeof, _Alignof, or _Generic", "example": "sizeof(i++)"},
        {"id": "EXP45-C", "title": "Do not perform assignments in selection statements", "example": "if (x = y)"},
        {"id": "EXP46-C", "title": "Do not use a bitwise operator with a Boolean-like operand", "example": "if (a & b) for bools"}
      ]
    },
    {
      "id": "INT",
      "name": "Integers",
      "rules": [
        {"id": "INT30-C", "title": "Ensure that unsigned integer operations do not wrap", "example": "unsigned overflow/underflow"},
        {"id": "INT31-C", "title": "Ensure that integer conversions do not result in lost or misinterpreted data", "example": "int to char truncation"},
        {"id": "INT32-C", "title": "Ensure that operations on signed integers do not result in overflow", "example": "INT_MAX + 1"},
        {"id": "INT33-C", "title": "Ensure that division and remainder operations do not result in divide-by-zero errors", "example": "x / 0 or x % 0"},
        {"id": "INT34-C", "title": "Do not shift an expression by a negative number of bits or by greater than or equal to the number of bits that exist in the operand", "example": "x << 32 or x << -1"},
        {"id": "INT35-C", "title": "Use correct integer precisions", "example": "sizeof(int) assumptions"},
        {"id": "INT36-C", "title": "Converting a pointer to integer or integer to pointer", "example": "(int)ptr or (int*)num"}
      ]
    },
    {
      "id": "FLP",
      "name": "Floating Point",
      "rules": [
        {"id": "FLP30-C", "title": "Do not use floating-point variables as loop counters", "example": "for (float f = 0; ...)"},
        {"id": "FLP32-C", "title": "Prevent or detect domain and range errors in math functions", "example": "sqrt(-1) or log(0)"},
        {"id": "FLP34-C", "title": "Ensure that floating-point conversions are within range of the new type", "example": "double to float overflow"},
        {"id": "FLP36-C", "title": "Preserve precision when converting integral values to floating-point type", "example": "large int to float"}
      ]
    },
    {
      "id": "ARR",
      "name": "Arrays",
      "rules": [
        {"id": "ARR30-C", "title": "Do not form or use out-of-bounds pointers or array subscripts", "example": "arr[size] or arr[-1]"},
        {"id": "ARR32-C", "title": "Ensure size arguments for variable length arrays are in a valid range", "example": "int arr[n] with n<=0"},
        {"id": "ARR36-C", "title": "Do not subtract or compare two pointers that do not refer to the same array", "example": "ptr1 - ptr2 different arrays"},
        {"id": "ARR37-C", "title": "Do not add or subtract an integer to a pointer to a non-array object", "example": "&scalar + 1"},
        {"id": "ARR38-C", "title": "Guarantee that library functions do not form invalid pointers", "example": "memcpy beyond bounds"},
        {"id": "ARR39-C", "title": "Do not add or subtract a scaled integer to a pointer", "example": "ptr + sizeof(int)*n"}
      ]
    },
    {
      "id": "STR",
      "name": "Characters and Strings",
      "rules": [
        {"id": "STR30-C", "title": "Do not attempt to modify string literals", "example": "char *s = \"lit\"; s[0] = 'x'"},
        {"id": "STR31-C", "title": "Guarantee that storage for strings has sufficient space for character data and the null terminator", "example": "strcpy to small buffer"},
        {"id": "STR32-C", "title": "Do not pass a non-null-terminated character sequence to a library function that expects a string", "example": "strlen(non_null_term)"},
        {"id": "STR34-C", "title": "Cast characters to unsigned char before converting to larger integer sizes", "example": "int c = getchar() sign extend"},
        {"id": "STR37-C", "title": "Arguments to character-handling functions must be representable as an unsigned char", "example": "isalpha(negative_char)"},
        {"id": "STR38-C", "title": "Do not confuse narrow and wide character strings and functions", "example": "wcslen on char*"}
      ]
    },
    {
      "id": "MEM",
      "name": "Memory Management",
      "rules": [
        {"id": "MEM30-C", "title": "Do not access freed memory", "example": "*ptr after free(ptr)"},
        {"id": "MEM31-C", "title": "Free dynamically allocated memory when no longer needed", "example": "malloc without free (leak)"},
        {"id": "MEM33-C", "title": "Allocate and copy structures containing a flexible array member dynamically", "example": "struct with FAM on stack"},
        {"id": "MEM34-C", "title": "Only free memory allocated dynamically", "example": "free(&stack_var)"},
        {"id": "MEM35-C", "title": "Allocate sufficient memory for an object", "example": "malloc(sizeof(int)) for long"},
        {"id": "MEM36-C", "title": "Do not modify the alignment of objects by calling realloc()", "example": "realloc aligned memory"}
      ]
    },
    {
      "id": "FIO",
      "name": "Input Output",
      "rules": [
        {"id": "FIO30-C", "title": "Exclude user input from format strings", "example": "printf(user_input)"},
        {"id": "FIO32-C", "title": "Do not perform operations on devices that are only appropriate for files", "example": "fseek on stdin"},
        {"id": "FIO34-C", "title": "Distinguish between characters read from a file and EOF or WEOF", "example": "char c = getchar(); c == EOF"},
        {"id": "FIO37-C", "title": "Do not assume that fgets() or fgetws() returns a nonempty string when successful", "example": "fgets then str[0] without check"},
        {"id": "FIO38-C", "title": "Do not copy a FILE object", "example": "FILE f2 = *f1"},
        {"id": "FIO39-C", "title": "Do not alternately input and output from a stream without an intervening flush or positioning call", "example": "fread then fwrite without fflush"},
        {"id": "FIO40-C", "title": "Reset strings on fgets() or fgetws() failure", "example": "use buffer after fgets fails"},
        {"id": "FIO41-C", "title": "Do not call getc(), putc(), getwc(), or putwc() with a stream argument that has side effects", "example": "getc(f++)"},
        {"id": "FIO42-C", "title": "Close files when they are no longer needed", "example": "fopen without fclose (leak)"},
        {"id": "FIO44-C", "title": "Only use values for fsetpos() that are returned from fgetpos()", "example": "fsetpos with arbitrary value"},
        {"id": "FIO45-C", "title": "Avoid TOCTOU race conditions while accessing files", "example": "access() then open()"},
        {"id": "FIO46-C", "title": "Do not access a closed file", "example": "fread after fclose"},
        {"id": "FIO47-C", "title": "Use valid format strings", "example": "printf(\"%s\", int_val)"}
      ]
    },
    {
      "id": "ENV",
      "name": "Environment",
      "rules": [
        {"id": "ENV30-C", "title": "Do not modify the object referenced by the return value of certain functions", "example": "modify getenv() result"},
        {"id": "ENV31-C", "title": "Do not rely on an environment pointer following an operation that may invalidate it", "example": "use envp after setenv"},
        {"id": "ENV32-C", "title": "All exit handlers must return normally", "example": "exit() in atexit handler"},
        {"id": "ENV33-C", "title": "Do not call system()", "example": "system(cmd)"},
        {"id": "ENV34-C", "title": "Do not store pointers returned by certain functions", "example": "store getenv() pointer"}
      ]
    },
    {
      "id": "SIG",
      "name": "Signals",
      "rules": [
        {"id": "SIG30-C", "title": "Call only asynchronous-safe functions within signal handlers", "example": "printf in signal handler"},
        {"id": "SIG31-C", "title": "Do not access shared objects in signal handlers", "example": "access global in handler"},
        {"id": "SIG34-C", "title": "Do not call signal() from within interruptible signal handlers", "example": "signal() inside handler"},
        {"id": "SIG35-C", "title": "Do not return from a computational exception signal handler", "example": "return from SIGFPE handler"}
      ]
    },
    {
      "id": "ERR",
      "name": "Error Handling",
      "rules": [
        {"id": "ERR30-C", "title": "Set errno to zero before calling a library function known to set errno, and check errno only after the function returns a value indicating failure", "example": "check errno without reset"},
        {"id": "ERR32-C", "title": "Do not rely on indeterminate values of errno", "example": "use errno after success"},
        {"id": "ERR33-C", "title": "Detect and handle standard library errors", "example": "ignore malloc return"}
      ]
    },
    {
      "id": "CON",
      "name": "Concurrency",
      "rules": [
        {"id": "CON30-C", "title": "Clean up thread-specific storage", "example": "tss_create without cleanup"},
        {"id": "CON31-C", "title": "Do not destroy a mutex while it is locked", "example": "mtx_destroy while locked"},
        {"id": "CON32-C", "title": "Prevent data races when accessing bit-fields from multiple threads", "example": "shared bit-field access"},
        {"id": "CON33-C", "title": "Avoid race conditions when using library functions", "example": "strtok in threads"},
        {"id": "CON34-C", "title": "Declare objects shared between threads with appropriate storage durations", "example": "thread access to local var"},
        {"id": "CON35-C", "title": "Avoid deadlock by locking in a predefined order", "example": "lock A then B vs B then A"},
        {"id": "CON36-C", "title": "Wrap functions that can spuriously wake up in a loop", "example": "cnd_wait without loop"},
        {"id": "CON37-C", "title": "Do not call signal() in a multithreaded program", "example": "signal() with threads"},
        {"id": "CON38-C", "title": "Preserve thread safety and liveness when using condition variables", "example": "cnd_signal without mutex"},
        {"id": "CON39-C", "title": "Do not join or detach a thread that was previously joined or detached", "example": "thrd_join twice"},
        {"id": "CON40-C", "title": "Do not refer to an atomic variable twice in an expression", "example": "atomic_var + atomic_var"},
        {"id": "CON41-C", "title": "Wrap functions that can fail spuriously in a loop", "example": "atomic_compare_exchange without loop"}
      ]
    },
    {
      "id": "MSC",
      "name": "Miscellaneous",
      "rules": [
        {"id": "MSC30-C", "title": "Do not use the rand() function for generating pseudorandom numbers", "example": "rand() for security"},
        {"id": "MSC32-C", "title": "Properly seed pseudorandom number generators", "example": "srand(1) or no seed"},
        {"id": "MSC33-C", "title": "Do not pass invalid data to the asctime() function", "example": "asctime with tm_year=9999"},
        {"id": "MSC37-C", "title": "Ensure that control never reaches the end of a non-void function", "example": "missing return in int func"},
        {"id": "MSC38-C", "title": "Do not treat a predefined identifier as an object if it might only be implemented as a macro", "example": "assert = 1"},
        {"id": "MSC39-C", "title": "Do not call va_arg() on a va_list that has an indeterminate value", "example": "va_arg after va_end"},
        {"id": "MSC40-C", "title": "Do not violate constraints", "example": "constraint violation"},
        {"id": "MSC41-C", "title": "Never hard code sensitive information", "example": "password = \"secret\""}
      ]
    },
    {
      "id": "POS",
      "name": "POSIX",
      "rules": [
        {"id": "POS30-C", "title": "Use the readlink() function properly", "example": "readlink without null-term"},
        {"id": "POS34-C", "title": "Do not call putenv() with a pointer to an automatic variable as the argument", "example": "putenv(local_str)"},
        {"id": "POS35-C", "title": "Avoid race conditions while checking for the existence of a symbolic link", "example": "lstat then open"},
        {"id": "POS36-C", "title": "Observe correct revocation order while relinquishing privileges", "example": "setuid before setgid"},
        {"id": "POS37-C", "title": "Ensure that privilege relinquishment is successful", "example": "ignore setuid return"},
        {"id": "POS38-C", "title": "Beware of race conditions when using fork and file descriptors", "example": "fork with shared fd"},
        {"id": "POS39-C", "title": "Use the correct byte ordering when transferring data between systems", "example": "send int without htonl"},
        {"id": "POS44-C", "title": "Do not use signals to terminate threads", "example": "pthread_kill to terminate"},
        {"id": "POS47-C", "title": "Do not use threads that can be canceled asynchronously", "example": "PTHREAD_CANCEL_ASYNCHRONOUS"},
        {"id": "POS48-C", "title": "Do not unlock or destroy another POSIX thread's mutex", "example": "unlock mutex from other thread"},
        {"id": "POS49-C", "title": "When data must be accessed by multiple threads, provide a mutex and guarantee no adjacent data is also accessed", "example": "false sharing"},
        {"id": "POS50-C", "title": "Declare objects shared between POSIX threads with appropriate storage durations", "example": "pthread access local var"},
        {"id": "POS51-C", "title": "Avoid deadlock with POSIX threads by locking in predefined order", "example": "pthread_mutex lock order"},
        {"id": "POS52-C", "title": "Do not perform operations that can block while holding a POSIX lock", "example": "I/O while mutex held"},
        {"id": "POS53-C", "title": "Do not use more than one mutex for concurrent waiting operations on a condition variable", "example": "cond_wait with different mutex"},
        {"id": "POS54-C", "title": "Detect and handle POSIX library errors", "example": "ignore pthread_create return"}
      ]
    }
  ]
}

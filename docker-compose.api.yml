services:
  certfix:
    build:
      context: .
    image: ghcr.io/safe-c-ai/certfix:api-local
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
    entrypoint: ["certfix"]
